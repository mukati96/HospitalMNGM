import { SidebarInterface } from "./sidebar.interface";

export interface ROLES {
    SUPERADMIN: SidebarInterface,
    PRACTICEADMIN: SidebarInterface,
    CAREMANAGER: SidebarInterface,
    PROVIDER: SidebarInterface,
    PATIENT: SidebarInterface,
}